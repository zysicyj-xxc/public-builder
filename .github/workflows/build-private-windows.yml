# Windows builds only. 与 build-private-android.yml 独立（独立 concurrency + 独立 release），
# 一个平台失败不影响另一个平台的构建/发布。
# Trust boundary: only repository_dispatch; treat client_payload as untrusted.

name: Public Builder Windows

on:
  repository_dispatch:
    types: [trigger-build]

permissions:
  contents: read

concurrency:
  group: pb-windows-${{ github.event.client_payload.mode }}-${{ github.event.client_payload.ref }}
  cancel-in-progress: false

defaults:
  run:
    shell: pwsh

jobs:
  validate:
    name: validate-payload
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      ref: ${{ steps.v.outputs.ref }}
      mode: ${{ steps.v.outputs.mode }}
      jobs: ${{ steps.v.outputs.jobs }}
      daymica_version: ${{ steps.v.outputs.daymica_version }}
      daymica_build: ${{ steps.v.outputs.daymica_build }}
      yaoxiaobai_version: ${{ steps.v.outputs.yaoxiaobai_version }}
      yaoxiaobai_build: ${{ steps.v.outputs.yaoxiaobai_build }}
      footprint_version: ${{ steps.v.outputs.footprint_version }}
      footprint_build: ${{ steps.v.outputs.footprint_build }}
      backend_url: ${{ steps.v.outputs.backend_url }}
      newapp_version: ${{ steps.v.outputs.newapp_version }}
      newapp_build: ${{ steps.v.outputs.newapp_build }}
    steps:
      - name: Validate client_payload
        id: v
        env:
          RAW_REF: ${{ github.event.client_payload.ref }}
          RAW_MODE: ${{ github.event.client_payload.mode }}
          RAW_JOBS: ${{ github.event.client_payload.jobs }}
          RAW_DAYMICA: ${{ github.event.client_payload.daymica }}
          RAW_YAOXIAOBAI: ${{ github.event.client_payload.yaoxiaobai }}
          RAW_FOOTPRINT: ${{ github.event.client_payload.footprint }}
          RAW_NEWAPP: ${{ github.event.client_payload.newapp }}
          RAW_BACKEND_URL: ${{ github.event.client_payload.backend_url }}
        run: |
          $ErrorActionPreference = 'Stop'
          function Out([string]$k, [string]$v) {
            "$k=$v" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8
          }
          function Assert-Match([string]$name, [string]$value, [string]$pattern) {
            if ([string]::IsNullOrWhiteSpace($value)) { throw "$name is empty" }
            if ($value -notmatch $pattern) { throw "$name failed allowlist: '$value'" }
            return $value
          }
          function Split-VerBuild([string]$name, [string]$value, [string]$vre, [string]$bre) {
            $t = if ($null -eq $value) { '' } else { $value.Trim() }
            if ([string]::IsNullOrWhiteSpace($t)) { return , @('0', '0') }
            $parts = $t -split '\+', 2
            $v = Assert-Match "${name}_version" $parts[0] $vre
            $b = if ($parts.Count -ge 2 -and $parts[1]) { Assert-Match "${name}_build" $parts[1] $bre } else { '0' }
            return , @($v, $b)
          }

          $ref = Assert-Match 'ref' $env:RAW_REF.Trim() '^refs/tags/(daymica|yaoxiaobai|footprint|backend|ai-usage|easy-shell|system-monitor|password-vault|clean-master|eye-care|issue-board)/(?:[0-9]{12}|[0-9]{4}\.[0-9]{1,2}\.[0-9]{1,2}-[0-9]+)$'
          $mode = Assert-Match 'mode' $env:RAW_MODE.Trim() '^release$'

          $allowedJobs = @(
            'daymica-windows', 'aiusage-windows', 'easyshell-windows',
            'systemmonitor-windows', 'passwordvault-windows', 'cleanmaster-windows', 'eyecare-windows',
            'issueboard-windows'
          )
          $jobs = @()
          foreach ($j in ($env:RAW_JOBS -split ',')) {
            $t = $j.Trim()
            if (-not $t) { continue }
            if ($t -notin $allowedJobs) { continue }
            if ($t -notin $jobs) { $jobs += $t }
          }
          if ($jobs.Count -eq 0) {
            Write-Host 'No windows jobs in payload; skip'
            Out 'ref' $ref
            Out 'mode' $mode
            Out 'jobs' ''
            exit 0
          }

          $verRe = '^[A-Za-z0-9._-]{1,64}$'
          $buildRe = '^[0-9]{1,16}$'

          $d = Split-VerBuild 'daymica' $env:RAW_DAYMICA $verRe $buildRe; $dv = $d[0]; $db = $d[1]
          $y = Split-VerBuild 'yaoxiaobai' $env:RAW_YAOXIAOBAI $verRe $buildRe; $yv = $y[0]; $yb = $y[1]
          $f = Split-VerBuild 'footprint' $env:RAW_FOOTPRINT $verRe $buildRe; $fv = $f[0]; $fb = $f[1]
          $n = Split-VerBuild 'newapp' $env:RAW_NEWAPP $verRe $buildRe; $nv = $n[0]; $nb = $n[1]
          $backendUrl = Assert-Match 'backend_url' $env:RAW_BACKEND_URL.Trim() '^https://[A-Za-z0-9.-]+(?::[0-9]{1,5})?(?:/[^\s]*)?$'

          $jobsCsv = ',' + ($jobs -join ',') + ','

          Write-Host "OK ref=$ref mode=$mode jobs=$($jobs -join ',')"
          Out 'ref' $ref
          Out 'mode' $mode
          Out 'jobs' $jobsCsv
          Out 'daymica_version' $dv
          Out 'daymica_build' $db
          Out 'yaoxiaobai_version' $yv
          Out 'yaoxiaobai_build' $yb
          Out 'footprint_version' $fv
          Out 'footprint_build' $fb
          Out 'backend_url' $backendUrl
          Out 'newapp_version' $nv
          Out 'newapp_build' $nb

  daymica-windows:
    name: daymica-windows
    needs: validate
    if: ${{ contains(needs.validate.outputs.jobs, ',daymica-windows,') }}
    runs-on: windows-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
        with:
          repository: zysicyj-xxc/zysicyj
          token: ${{ secrets.PRIVATE_REPO_ACCESS_TOKEN }}
          ref: ${{ needs.validate.outputs.ref }}
          persist-credentials: false
          fetch-depth: 1
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.8'
          channel: 'stable'
          cache: true
      # Flutter SDK + pub cache 由 subosito/flutter-action 的 cache: true 覆盖（实测命中），
      # 不再显式重复缓存（避免与 subosito 缓存重复占用 10GB 仓库配额）。
      - name: Cache build + dart_tool
        uses: actions/cache@v4
        with:
          path: |
            apps/daymica/.dart_tool
            apps/shared_package/.dart_tool
            apps/daymica/build
          key: ${{ runner.os }}-windows-daymica-${{ hashFiles('apps/daymica/pubspec.lock') }}
          restore-keys: |
            ${{ runner.os }}-windows-daymica-
            ${{ runner.os }}-windows-
      - name: build
        env:
          APP_VERSION: ${{ needs.validate.outputs.daymica_version }}
          APP_BUILD: ${{ needs.validate.outputs.daymica_build }}
          DAYMICA_RELEASE_TOKEN: ${{ secrets.DAYMICA_RELEASE_TOKEN }}
          RUSTFS_ACCESS_KEY: ${{ secrets.DAYMICA_RUSTFS_ACCESS_KEY }}
          RUSTFS_SECRET_KEY: ${{ secrets.DAYMICA_RUSTFS_SECRET_KEY }}
          DAYMICA_RUSTFS_ACCESS_KEY: ${{ secrets.DAYMICA_RUSTFS_ACCESS_KEY }}
          DAYMICA_RUSTFS_SECRET_KEY: ${{ secrets.DAYMICA_RUSTFS_SECRET_KEY }}
        run: |
          $ErrorActionPreference = 'Stop'
          if ($env:APP_BUILD -notmatch '^[0-9]{1,16}$') { throw 'invalid APP_BUILD' }
          & ./scripts/build-flutter-windows-prod.ps1 -AppSlug daymica -Version $env:APP_VERSION -BuildNumber ([int]$env:APP_BUILD)
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

  aiusage-windows:
    name: aiusage-windows
    needs: validate
    if: ${{ contains(needs.validate.outputs.jobs, ',aiusage-windows,') }}
    runs-on: windows-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
        with:
          repository: zysicyj-xxc/zysicyj
          token: ${{ secrets.PRIVATE_REPO_ACCESS_TOKEN }}
          ref: ${{ needs.validate.outputs.ref }}
          persist-credentials: false
          fetch-depth: 1
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.8'
          channel: 'stable'
          cache: true
      - name: Cache build + dart_tool
        uses: actions/cache@v4
        with:
          path: |
            apps/ai-usage/.dart_tool
            apps/shared_package/.dart_tool
            apps/ai-usage/build
          key: ${{ runner.os }}-windows-aiusage-${{ hashFiles('apps/ai-usage/pubspec.lock') }}
          restore-keys: |
            ${{ runner.os }}-windows-aiusage-
            ${{ runner.os }}-windows-
      - name: build
        env:
          APP_VERSION: ${{ needs.validate.outputs.newapp_version }}
          APP_BUILD: ${{ needs.validate.outputs.newapp_build }}
          DAYMICA_RELEASE_TOKEN: ${{ secrets.DAYMICA_RELEASE_TOKEN }}
          RUSTFS_ACCESS_KEY: ${{ secrets.DAYMICA_RUSTFS_ACCESS_KEY }}
          RUSTFS_SECRET_KEY: ${{ secrets.DAYMICA_RUSTFS_SECRET_KEY }}
          DAYMICA_RUSTFS_ACCESS_KEY: ${{ secrets.DAYMICA_RUSTFS_ACCESS_KEY }}
          DAYMICA_RUSTFS_SECRET_KEY: ${{ secrets.DAYMICA_RUSTFS_SECRET_KEY }}
        run: |
          $ErrorActionPreference = 'Stop'
          if ($env:APP_BUILD -notmatch '^[0-9]{1,16}$') { throw 'invalid APP_BUILD' }
          & ./scripts/build-aiusage-windows-prod.ps1 -Version $env:APP_VERSION -BuildNumber ([int]$env:APP_BUILD)
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

  easyshell-windows:
    name: easyshell-windows
    needs: validate
    if: ${{ contains(needs.validate.outputs.jobs, ',easyshell-windows,') }}
    runs-on: windows-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
        with:
          repository: zysicyj-xxc/zysicyj
          token: ${{ secrets.PRIVATE_REPO_ACCESS_TOKEN }}
          ref: ${{ needs.validate.outputs.ref }}
          persist-credentials: false
          fetch-depth: 1
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.8'
          channel: 'stable'
          cache: true
      - name: Cache build + dart_tool
        uses: actions/cache@v4
        with:
          path: |
            apps/easy-shell/.dart_tool
            apps/shared_package/.dart_tool
            apps/easy-shell/build
          key: ${{ runner.os }}-windows-easyshell-${{ hashFiles('apps/easy-shell/pubspec.lock') }}
          restore-keys: |
            ${{ runner.os }}-windows-easyshell-
            ${{ runner.os }}-windows-
      - name: build
        env:
          APP_VERSION: ${{ needs.validate.outputs.newapp_version }}
          APP_BUILD: ${{ needs.validate.outputs.newapp_build }}
          DAYMICA_RELEASE_TOKEN: ${{ secrets.DAYMICA_RELEASE_TOKEN }}
          RUSTFS_ACCESS_KEY: ${{ secrets.DAYMICA_RUSTFS_ACCESS_KEY }}
          RUSTFS_SECRET_KEY: ${{ secrets.DAYMICA_RUSTFS_SECRET_KEY }}
          DAYMICA_RUSTFS_ACCESS_KEY: ${{ secrets.DAYMICA_RUSTFS_ACCESS_KEY }}
          DAYMICA_RUSTFS_SECRET_KEY: ${{ secrets.DAYMICA_RUSTFS_SECRET_KEY }}
        run: |
          $ErrorActionPreference = 'Stop'
          if ($env:APP_BUILD -notmatch '^[0-9]{1,16}$') { throw 'invalid APP_BUILD' }
          & ./scripts/build-easyshell-windows-prod.ps1 -Version $env:APP_VERSION -BuildNumber ([int]$env:APP_BUILD)
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

  systemmonitor-windows:
    name: systemmonitor-windows
    needs: validate
    if: ${{ contains(needs.validate.outputs.jobs, ',systemmonitor-windows,') }}
    runs-on: windows-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
        with:
          repository: zysicyj-xxc/zysicyj
          token: ${{ secrets.PRIVATE_REPO_ACCESS_TOKEN }}
          ref: ${{ needs.validate.outputs.ref }}
          persist-credentials: false
          fetch-depth: 1
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.8'
          channel: 'stable'
          cache: true
      - name: Cache build + dart_tool
        uses: actions/cache@v4
        with:
          path: |
            apps/system-monitor/.dart_tool
            apps/shared_package/.dart_tool
            apps/system-monitor/build
          key: ${{ runner.os }}-windows-systemmonitor-${{ hashFiles('apps/system-monitor/pubspec.lock') }}
          restore-keys: |
            ${{ runner.os }}-windows-systemmonitor-
            ${{ runner.os }}-windows-
      - name: build
        env:
          APP_VERSION: ${{ needs.validate.outputs.newapp_version }}
          APP_BUILD: ${{ needs.validate.outputs.newapp_build }}
          DAYMICA_RELEASE_TOKEN: ${{ secrets.DAYMICA_RELEASE_TOKEN }}
          RUSTFS_ACCESS_KEY: ${{ secrets.DAYMICA_RUSTFS_ACCESS_KEY }}
          RUSTFS_SECRET_KEY: ${{ secrets.DAYMICA_RUSTFS_SECRET_KEY }}
          DAYMICA_RUSTFS_ACCESS_KEY: ${{ secrets.DAYMICA_RUSTFS_ACCESS_KEY }}
          DAYMICA_RUSTFS_SECRET_KEY: ${{ secrets.DAYMICA_RUSTFS_SECRET_KEY }}
        run: |
          $ErrorActionPreference = 'Stop'
          if ($env:APP_BUILD -notmatch '^[0-9]{1,16}$') { throw 'invalid APP_BUILD' }
          & ./scripts/build-systemmonitor-windows-prod.ps1 -Version $env:APP_VERSION -BuildNumber ([int]$env:APP_BUILD)
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

  passwordvault-windows:
    name: passwordvault-windows
    needs: validate
    if: ${{ contains(needs.validate.outputs.jobs, ',passwordvault-windows,') }}
    runs-on: windows-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
        with:
          repository: zysicyj-xxc/zysicyj
          token: ${{ secrets.PRIVATE_REPO_ACCESS_TOKEN }}
          ref: ${{ needs.validate.outputs.ref }}
          persist-credentials: false
          fetch-depth: 1
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.8'
          channel: 'stable'
          cache: true
      - name: Cache build + dart_tool
        uses: actions/cache@v4
        with:
          path: |
            apps/password-vault/.dart_tool
            apps/shared_package/.dart_tool
            apps/password-vault/build
          key: ${{ runner.os }}-windows-passwordvault-${{ hashFiles('apps/password-vault/pubspec.lock') }}
          restore-keys: |
            ${{ runner.os }}-windows-passwordvault-
            ${{ runner.os }}-windows-
      - name: build
        env:
          APP_VERSION: ${{ needs.validate.outputs.newapp_version }}
          APP_BUILD: ${{ needs.validate.outputs.newapp_build }}
          DAYMICA_RELEASE_TOKEN: ${{ secrets.DAYMICA_RELEASE_TOKEN }}
          RUSTFS_ACCESS_KEY: ${{ secrets.DAYMICA_RUSTFS_ACCESS_KEY }}
          RUSTFS_SECRET_KEY: ${{ secrets.DAYMICA_RUSTFS_SECRET_KEY }}
          DAYMICA_RUSTFS_ACCESS_KEY: ${{ secrets.DAYMICA_RUSTFS_ACCESS_KEY }}
          DAYMICA_RUSTFS_SECRET_KEY: ${{ secrets.DAYMICA_RUSTFS_SECRET_KEY }}
        run: |
          $ErrorActionPreference = 'Stop'
          if ($env:APP_BUILD -notmatch '^[0-9]{1,16}$') { throw 'invalid APP_BUILD' }
          & ./scripts/build-passwordvault-windows-prod.ps1 -Version $env:APP_VERSION -BuildNumber ([int]$env:APP_BUILD)
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

  cleanmaster-windows:
    name: cleanmaster-windows
    needs: validate
    if: ${{ contains(needs.validate.outputs.jobs, ',cleanmaster-windows,') }}
    runs-on: windows-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
        with:
          repository: zysicyj-xxc/zysicyj
          token: ${{ secrets.PRIVATE_REPO_ACCESS_TOKEN }}
          ref: ${{ needs.validate.outputs.ref }}
          persist-credentials: false
          fetch-depth: 1
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.8'
          channel: 'stable'
          cache: true
      - name: Cache build + dart_tool
        uses: actions/cache@v4
        with:
          path: |
            apps/clean-master/.dart_tool
            apps/shared_package/.dart_tool
            apps/clean-master/build
          key: ${{ runner.os }}-windows-cleanmaster-${{ hashFiles('apps/clean-master/pubspec.lock') }}
          restore-keys: |
            ${{ runner.os }}-windows-cleanmaster-
            ${{ runner.os }}-windows-
      - name: build
        env:
          APP_VERSION: ${{ needs.validate.outputs.newapp_version }}
          APP_BUILD: ${{ needs.validate.outputs.newapp_build }}
          DAYMICA_RELEASE_TOKEN: ${{ secrets.DAYMICA_RELEASE_TOKEN }}
          RUSTFS_ACCESS_KEY: ${{ secrets.DAYMICA_RUSTFS_ACCESS_KEY }}
          RUSTFS_SECRET_KEY: ${{ secrets.DAYMICA_RUSTFS_SECRET_KEY }}
          DAYMICA_RUSTFS_ACCESS_KEY: ${{ secrets.DAYMICA_RUSTFS_ACCESS_KEY }}
          DAYMICA_RUSTFS_SECRET_KEY: ${{ secrets.DAYMICA_RUSTFS_SECRET_KEY }}
        run: |
          $ErrorActionPreference = 'Stop'
          if ($env:APP_BUILD -notmatch '^[0-9]{1,16}$') { throw 'invalid APP_BUILD' }
          & ./scripts/build-cleanmaster-windows-prod.ps1 -Version $env:APP_VERSION -BuildNumber ([int]$env:APP_BUILD)
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

  eyecare-windows:
    name: eyecare-windows
    needs: validate
    if: ${{ contains(needs.validate.outputs.jobs, ',eyecare-windows,') }}
    runs-on: windows-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
        with:
          repository: zysicyj-xxc/zysicyj
          token: ${{ secrets.PRIVATE_REPO_ACCESS_TOKEN }}
          ref: ${{ needs.validate.outputs.ref }}
          persist-credentials: false
          fetch-depth: 1
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.8'
          channel: 'stable'
          cache: true
      - name: Cache build + dart_tool
        uses: actions/cache@v4
        with:
          path: |
            apps/eye-care/.dart_tool
            apps/shared_package/.dart_tool
            apps/eye-care/build
          key: ${{ runner.os }}-windows-eyecare-${{ hashFiles('apps/eye-care/pubspec.lock') }}
          restore-keys: |
            ${{ runner.os }}-windows-eyecare-
            ${{ runner.os }}-windows-
      - name: build
        env:
          APP_VERSION: ${{ needs.validate.outputs.newapp_version }}
          APP_BUILD: ${{ needs.validate.outputs.newapp_build }}
          DAYMICA_RELEASE_TOKEN: ${{ secrets.DAYMICA_RELEASE_TOKEN }}
          RUSTFS_ACCESS_KEY: ${{ secrets.DAYMICA_RUSTFS_ACCESS_KEY }}
          RUSTFS_SECRET_KEY: ${{ secrets.DAYMICA_RUSTFS_SECRET_KEY }}
          DAYMICA_RUSTFS_ACCESS_KEY: ${{ secrets.DAYMICA_RUSTFS_ACCESS_KEY }}
          DAYMICA_RUSTFS_SECRET_KEY: ${{ secrets.DAYMICA_RUSTFS_SECRET_KEY }}
        run: |
          $ErrorActionPreference = 'Stop'
          if ($env:APP_BUILD -notmatch '^[0-9]{1,16}$') { throw 'invalid APP_BUILD' }
          & ./scripts/build-eyecare-windows-prod.ps1 -Version $env:APP_VERSION -BuildNumber ([int]$env:APP_BUILD)
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

  issueboard-windows:
    name: issueboard-windows
    needs: validate
    if: ${{ contains(needs.validate.outputs.jobs, ',issueboard-windows,') }}
    runs-on: windows-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
        with:
          repository: zysicyj-xxc/zysicyj
          token: ${{ secrets.PRIVATE_REPO_ACCESS_TOKEN }}
          ref: ${{ needs.validate.outputs.ref }}
          persist-credentials: false
          fetch-depth: 1
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.8'
          channel: 'stable'
          cache: true
      - name: Ensure Inno Setup
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $iscc = @(
            $env:INNO_SETUP_ISCC,
            (Join-Path ${env:ProgramFiles(x86)} 'Inno Setup 6\ISCC.exe'),
            (Join-Path $env:ProgramFiles 'Inno Setup 6\ISCC.exe'),
            (Join-Path $env:LOCALAPPDATA 'Programs\Inno Setup 6\ISCC.exe')
          ) | Where-Object { $_ -and (Test-Path -LiteralPath $_) } | Select-Object -First 1
          if (-not $iscc) {
            choco install innosetup --no-progress -y
          }
      - name: Cache build + dart_tool
        uses: actions/cache@v4
        with:
          path: |
            apps/issue-board/.dart_tool
            apps/shared_package/.dart_tool
            apps/issue-board/build
          key: ${{ runner.os }}-windows-issueboard-${{ hashFiles('apps/issue-board/pubspec.lock') }}
          restore-keys: |
            ${{ runner.os }}-windows-issueboard-
            ${{ runner.os }}-windows-
      - name: build
        env:
          APP_VERSION: ${{ needs.validate.outputs.newapp_version }}
          APP_BUILD: ${{ needs.validate.outputs.newapp_build }}
          DAYMICA_RELEASE_TOKEN: ${{ secrets.DAYMICA_RELEASE_TOKEN }}
          RUSTFS_ACCESS_KEY: ${{ secrets.DAYMICA_RUSTFS_ACCESS_KEY }}
          RUSTFS_SECRET_KEY: ${{ secrets.DAYMICA_RUSTFS_SECRET_KEY }}
          DAYMICA_RUSTFS_ACCESS_KEY: ${{ secrets.DAYMICA_RUSTFS_ACCESS_KEY }}
          DAYMICA_RUSTFS_SECRET_KEY: ${{ secrets.DAYMICA_RUSTFS_SECRET_KEY }}
        run: |
          $ErrorActionPreference = 'Stop'
          if ($env:APP_BUILD -notmatch '^[0-9]{1,16}$') { throw 'invalid APP_BUILD' }
          & ./scripts/build-issueboard-windows-prod.ps1 -Version $env:APP_VERSION -BuildNumber ([int]$env:APP_BUILD)
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

  # ---------------------------------------------------------------------------
  # Create GitHub Release after all windows builds succeed (idempotent).
  # Artifacts live on RustFS; this release serves as version record + download index.
  # ---------------------------------------------------------------------------
  release:
    name: create-github-release
    needs: [validate, daymica-windows, aiusage-windows, easyshell-windows, systemmonitor-windows, passwordvault-windows, cleanmaster-windows, eyecare-windows, issueboard-windows]
    if: ${{ always() && needs.validate.result == 'success' && needs.validate.outputs.jobs != '' && !contains(needs.*.result, 'failure') && !contains(needs.*.result, 'cancelled') }}
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Create Release on private repo
        shell: pwsh
        env:
          GH_TOKEN: ${{ secrets.PRIVATE_REPO_ACCESS_TOKEN }}
          REF: ${{ needs.validate.outputs.ref }}
          JOBS: ${{ needs.validate.outputs.jobs }}
        run: |
          $ErrorActionPreference = 'Stop'
          $tagName = $env:REF -replace '^refs/tags/', ''
          $app = ($tagName -split '/')[0]
          $ver = ($tagName -split '/', 2)[1] -replace '-[0-9]+$', ''
          $jobsList = $env:JOBS.Trim(',')

          # 幂等 + 合并：同 tag 可能被 android/windows 两个 workflow 并发创建。
          # 已存在时读取现有 notes，把本 workflow 的 jobs 合并进去（去重）后更新，
          # 保证最终 release notes 包含全部平台（如 windows 先创建、android 后合并）。
          $existing = gh release view $tagName --repo zysicyj-xxc/zysicyj 2>$null
          if ($LASTEXITCODE -ne 0) {
            $notes = "## $app v$ver`n`n**Tag:** ``$tagName```n**Built jobs:** $jobsList`n`nDownloads are hosted on RustFS (https://rustfs.zysicyj.top)."
            gh release create $tagName --repo zysicyj-xxc/zysicyj --title "$app v$ver" --notes $notes --latest
            if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
            Write-Host "Created release: $tagName"
          } else {
            $oldNotes = $existing.body
            $jobsLine = ($oldNotes -split "`n" | Where-Object { $_ -match '^\*\*Built jobs:\*\*' } | Select-Object -First 1)
            if ($jobsLine) {
              $oldJobs = @(($jobsLine -replace '^\*\*Built jobs:\*\*\s*', '') -split ', ' | Where-Object { $_ })
              $myJobs = @($jobsList -split ', ' | Where-Object { $_ })
              $merged = @($oldJobs + $myJobs | Select-Object -Unique) -join ', '
              $newNotes = $oldNotes -replace [regex]::Escape($jobsLine), "**Built jobs:** $merged"
            } else {
              $newNotes = $oldNotes + "`n`n**Built jobs:** $jobsList"
            }
            gh release edit $tagName --repo zysicyj-xxc/zysicyj --notes $newNotes
            if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
            Write-Host "Merged jobs into existing release: $tagName"
          }
